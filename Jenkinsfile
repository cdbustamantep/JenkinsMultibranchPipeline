pipeline {
	agent any
		stages {
			stage('One') {
				steps { sh 'echo "Step Uno"'
				}
			}
			stage('Two') {
				steps { sh 'echo "Step dos"'
				}
			} 
			stage('Three') {
				steps { sh 'echo "Step Three"'
				}
			}
		}
}

