pipeline {
	agent any
		stages {
			stage('One') {
				steps { sh 'echo "Step Uno"'
				}
			}
			stage('Two') {
				steps { sh 'echo "Step Two"'
				}
			} 
			stage('Three') {
				steps { sh 'echo "Step Three"'
				}
			}
		}
}

